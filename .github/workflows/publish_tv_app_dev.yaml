name: tv-app-build-action-dev-disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android TV dev build is temporarily disabled. Current target platforms are iPhone, iPad, and macOS."
